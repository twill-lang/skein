# Written by spool. Reproduced from spool.toml and spool.lock, so it is not
# source and does not belong in a commit.
twill_modules/
.spool/

# Trained artefacts. A vocabulary and a merge list are outputs of a corpus, and
# the corpus is what belongs in version control.
*.vocab
*.merges
